---
apiVersion: v1
kind: Secret
metadata:
  name: mouse-secrets
type: Opaque
data:
  # Note: These should be base64 encoded. You can encode them using:
  # echo -n "postgresql://user:password@host:5432/dbname" | base64
  # echo -n "your-openai-api-key" | base64
  DB_PASSWORD: CHANGEME
  OPENAI_API_KEY: CHANGEME